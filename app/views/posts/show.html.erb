<%= image_tag @post.main_image %>
<p>Caption: <strong><%= @post.caption %></strong></p>

<div class="col-md-7">
    <h4>Comments</h4>
    <% @comments.each do |f| %>

        <p><%= f.user.username %>: <%= f.description %></p>

    <% end %>
</div>

<%= render 'comments/form' %>